import { PowerHighlighterDirective } from './power-highlighter.directive';

describe('PowerHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new PowerHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
